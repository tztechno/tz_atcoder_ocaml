//abc098_a.ml
##########################################
##########################################
##########################################
##########################################
##########################################
##########################################
##########################################
##########################################
let _ = Scanf.sscanf (read_line ()) "%d %d" (fun a b ->
  print_int @@ max (a * b) @@ max (a + b) (a - b)
)

##########################################
let () =
  let a, b = Scanf.sscanf (read_line ()) "%d %d" (fun a b -> (a, b)) in
  let ans = max (a+b) (max (a-b) (a*b)) in
  Printf.printf "%d\n" ans

##########################################
let a, b = Scanf.sscanf (read_line ()) "%d %d" (fun a b -> (a, b))
let s = [a+b; a-b; a*b]
let ans = List.fold_left max (List.hd s) s
let () = print_endline (string_of_int ans)
##########################################
